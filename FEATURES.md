# Super High 功能讲解

本文逐项讲解 Super High 的功能与用法。面向使用该应用的开发者，读完可以了解每个模块「能做什么、怎么用」。

- 相关文档：[README.md](README.md) ｜ [CLI.md](CLI.md)

---

## 1. 工作区与项目管理

Super High 以「工作区」为单位组织内容，一个工作区就是一个本地项目文件夹。

- **打开项目**：顶部栏「文件 > 打开工作区」，或直接把文件夹拖入。
- **最近项目**：欢迎页和「文件」菜单里列出最近打开的项目，一键恢复。
- **多标签**：一个工作区可以在「项目 / 文档」等标签间切换。
- **项目说明文件**：自动识别项目根目录的说明文件（如 `AGENTS.md`、`CLAUDE.md`），并支持从旧命名迁移到新命名，方便 AI 工具读取项目约定。

## 2. 代码编辑

- **多标签编辑器**：基于 Monaco，支持语法高亮、撤销 / 重做、剪切 / 复制 / 粘贴、全选、保存、关闭等标准操作。
- **文件树**：左侧 Explorer 以树状展示项目文件，支持面包屑导航、展开 / 折叠、刷新；文件类型自动识别语言。
- **项目内搜索**：顶部栏搜索框在当前项目内全文搜索，结果只显示项目内相对路径，避免泄露本机绝对路径。
- **预览能力**：
  - Markdown 实时预览
  - YAML 结构化预览
  - 图片与音视频预览（后端以 data URL 返回，安全策略已允许）
  - 颜色代码预览（识别文本里的颜色码并渲染色块）
- **剪贴板图片**：粘贴图片到编辑器时可保存为项目文件，并自动处理格式。

## 3. 文件管理

- **文件管理器**：独立面板，支持多标签打开多个目录。
- **常用操作**：新建文件 / 目录、读写、重命名、删除、复制、移动；复制路径、导入文件到当前目录、粘贴本机剪贴板文件。
- 文件操作走 Rust 后端，处理 Windows 长路径与扩展路径前缀等细节。

## 4. 内嵌终端

- **本地终端**：基于 xterm 的交互式会话，支持调整大小、回读缓冲区、关闭；可为一个工作区开多个终端。
- **外部 CLI 会话**：在终端里启动 AI 命令行工具（检测到本机已安装的 Codex CLI、Claude Code 等），并在界面里渲染其专用终端协议输出。
- **SSH 会话**：设置 > SSH 连接 里保存服务器连接，点击连接即在终端打开交互式 SSH 会话。
- **终端动作**：通过 `.superhigh/terminal-actions.json` 注册动作按钮，一键向项目终端发送命令、切换服务或重启项目服务（见第 5 节）。

## 5. 项目服务启动

适合「一个项目要同时拉起多个进程」的场景（例如数据库、中间件、主程序）。配置在 `.superhigh/project.json`：

- **单脚本模式**：只指定一个启动脚本。
- **服务编排模式**：声明多个服务，支持：
  - `startAfter`：服务间的启动依赖顺序
  - `healthCheck`：TCP 健康检查（host + port），就绪后才认为启动成功
  - 状态实时反馈：Starting / Running / Failed 等状态会推送到界面
  - 每个服务可以挂在独立的终端会话里，便于查看日志

示例：

```json
{
  "version": 1,
  "startup": {
    "name": "Stack",
    "services": [
      { "id": "main", "script": "../main.bat", "workingDirectory": "..", "startAfter": ["mysql", "redis"], "healthCheck": { "type": "tcp", "port": 32271 } },
      { "id": "mysql", "command": "../mysql.exe", "workingDirectory": "..", "healthCheck": { "type": "tcp", "port": 3306 } },
      { "id": "redis", "script": "../redis.bat", "workingDirectory": ".." }
    ]
  }
}
```

## 6. 插件系统

Super High 加载「受信任的本机 JavaScript 插件」：

- 全局：`%APPDATA%\SuperHigh\plugins\`
- 项目：`<工作区>\.superhigh\plugins\`

打开工作区时自动加载该项目的插件；同一个插件 id 全局与项目并存时以项目为准。

- **插件描述**：每个插件目录包含 `plugin.json`（id、名称、命令等），文件夹名必须与 id 一致。
- **命令注入**：插件可以声明命令，注入到活动栏，点击即运行。
- **能力**：读取插件资源、执行本机命令（shell exec），并有独立的 UI 运行时面板。
- **禁用与恢复**：`--disable-plugins` 启动参数，或在应用数据目录放置 `disable-plugins.flag`，可临时跳过插件加载；删除标记文件即恢复。
- 插件是本机全权限代码，安装即表示信任。

## 7. 脚本工具

把项目里常用的脚本变成界面按钮。配置 `.superhigh/script-tools.json`：

- 注册允许的目录（工作区相对路径），脚本必须位于这些目录内。
- 每个工具声明：真实脚本路径、运行时（`python` / `powershell` / `node` / `cmd`）、输入字段、类型。
- 类型分两种：`query`（只读查询，直接运行）与 `maintenance`（维护操作，需二次确认）。
- **输出渲染**：脚本输出用 Markdown 渲染；支持可点击的文件引用（`路径:行` 或 `路径:行:列`，点击在代码预览中跳转）；YAML 代码块自动语法高亮。
- 也支持给工具绑定自定义 HTML 界面（`ui` 字段），通过注入的脚本工具接口渲染结果。

## 8. 对话与 AI CLI

- **对话管理**：左侧对话侧栏管理多个 AI CLI 会话，支持重命名、新增对话。
- **环境检测**：自动检测本机已安装的 CLI 工具（如 Codex CLI、Claude Code），在终端中直接启动。
- **选中提问**：在代码预览里选中代码片段，可快捷生成带路径与行号的提问内容。
- **语音输入**：支持系统语音识别输入与快捷键呼出，语音结果可直接进入输入框。
- **计划解析**：识别 AI 输出的计划文本，便于拆解为任务卡片（配合 CLI 的 cards 使用）。

## 9. 渠道检测

针对使用 AI 渠道（接口网关）的场景：

- 探测已配置渠道的连通性、延迟（网站延迟探测）。
- 拉取渠道支持的模型列表，方便确认可用模型。
- 渠道配置保存在 设置 中，支持增删。

## 10. 备忘录

- 独立便签窗口，可常驻屏幕角落随手记录。
- 内容保存在本地数据库（增 / 删 / 改 / 查），不依赖网络。

## 11. 文档预览

- 自动检测工作区内是否存在文档站点目录（VitePress 结构）。
- 顶部栏一键启动 / 停止文档预览，启动后给出访问地址；也可以在「文档」工作区标签里阅读。

## 12. 项目编辑器

为项目提供「专属界面」：

- 在 `.superhigh/editor/` 放入 HTML 文件，Super High 把它当作项目的自定义面板。
- 页面挂载进 Shadow DOM，样式与主界面隔离。
- 页面脚本在隔离作用域执行，不能使用内联事件；交互通过 `addEventListener` 完成。
- 通过 `superhigh-editor:fs` 等安全接口分页读取项目文件、访问物品卡片等资源。

## 13. 手机端

- 在局域网内启动「移动端宿主」，手机浏览器访问同一工作区。
- 移动端页面（`src/mobile`）提供为手机优化的界面。
- 提供构建移动端页面与打包 APK 的脚本。

## 14. 原生 CLI

`superhigh-cli` 是面向 AI 与脚本的原生命令行入口，复用 Rust 后端逻辑，而不是重复 GUI 行为：

```powershell
npm run cli -- --help
```

常用命令：

- `cards`：任务卡片管理（list / create / show / claim / update / set-status / delete）
- `docs`：文档图 / 校验（graph / check）
- `files`：文件操作（ls / read / delete）
- `skills`：读取项目 skills 与说明文件（project list / instructions list / global workspace）
- `env`：输出当前 CLI 环境
- `search`：项目内搜索

说明：

- 所有命令都接受 `--project <path>`（缺省用当前目录），加 `--json` 输出机器可读结果。
- 破坏性命令需要显式 `--force` 才执行。
- 更多示例见 [CLI.md](CLI.md)。

## 15. 数据存储与主题

- **本地数据库**：设置、最近项目、SSH 连接、备忘录等都存在本机应用数据目录（SQLite），不依赖云端。
- **主题**：设置 > 外观主题 即时切换并保存；主题目录可扩展。
- 数据存储面板展示数据库路径与占用大小，可一键打开所在目录。

## 17. 系统集成

- **Windows 右键菜单**：提供注册 / 注销脚本，把「用 Super High 打开」加入文件夹右键菜单。
- **打开路径 / URL**：安全地打开本地路径与外部网址（仅允许 http/https，并做净化）。
- **应用关闭**：提供关机入口，安全结束所有子进程。

---

> 以上功能以公开版为准。若某模块在你的版本里不可见，请确认构建与启动方式（见 README）。
