# Super High 协作约束

## 执行与交付

- 固定 Vue 3、Vite、Tauri 2、Rust；系统、文件、进程和窗口能力放在 `src-tauri`。
- 说明用中文；只改任务必需内容，不扩展多人、多机或产品级安全工程。
- 默认执行一个最相关检查，失败或存在未覆盖风险时再补充；纯指令整理只核对内容。
- 桌面 UI 修改遵循 [UI 约束](.codex/skills/super-high-ui-constraints/SKILL.md)；浏览器实时预览、交互模拟和场景参数使用 [superhigh-html](.codex/skills/superhigh-html/SKILL.md)。
- 功能修改、修复、清理及发布配置任务开始前，读取 [superhigh-github](.codex/skills/superhigh-github/SKILL.md)。纯桌面 UI 修改默认本机验收并同步脱敏源码；其他功能修改、修复和清理还须发布新版 Release。说明、技能或发布配置调整只核对并同步源码。用户指定范围优先。
- 安装包和 APK 默认从脱敏公开源码在本机构建；云端构建须用户明确要求。交付说明本机、GitHub 源码和 Release 的实际状态。

## 应用与会话保护

- 修改 Rust、Tauri 命令或打包配置后，按 [README Build](README.md#build) 构建正式版，并遵守会话保护后重启验收。
- 内部会话以 `TerminalManager` 创建、仍持有 PTY 子进程且登记在终端会话表中为准。存在内部会话时，不得关闭、重启应用或打断子进程；确需重启须说明受影响会话并取得用户明确同意。
- 未登记的外部命令行进程不阻碍重启，也不得干预。
- 更新用户实际启动入口使用的程序，从该入口启动并核对运行 EXE 与本次产物一致，验收目标交互；未重启或未覆盖原问题场景时须明确说明。

## 配置边界

- cc-switch 数据库只读；dsh 以 `~/.dsh/config.toml` 为唯一编辑源，兼容配置须保留无关字段和注释。
- DSH Key 仅来自 `config.toml`；`SUPERHIGH_DSH_KEY_*` 仅注入当前 DSH 子进程，禁止写入 Windows 用户或机器环境变量。

## DSH 固定边界

- 私有交互界面固定为 `~/.dsh/profiles/dsh-tui`，包名 `dsh-tui`、版本 `0.4.1`；DSH 主程序固定 `0.1.2-rc.1`。
- 未经用户在当前请求中逐项授权，禁止外部安装、升级、降级、替换或重建上述程序，以及会改写该配置目录的插件更新、依赖安装或初始化；不得引入旧名称或外部同名界面。
- 注册命令、别名、PATH 或包装时，只改独立包装层，原样转发到现场已有的 `dsh --profile dsh-tui`；不得修改包身份、目录联接、配置目录映射或配置文件。
- 版本和映射以现场文件及命令输出为准，不得根据历史记录恢复、迁移或改链。入口注册暴露版本不兼容时，报告错误并停止，不扩大为版本或配置修复。

## 协作与收尾

- 清理本次已确认无用的临时产物；大量生成或搬移时使用 `agent-cleaner`，核对精确路径后移入回收站。
- 保留当前 `node_modules`、在用正式版、源码、日志、未跟踪交接材料及尚未确认无用的素材、会话和证据。
